{
  "version": 2,
  "buildCommand": "PYTHONPATH=.vercel_python_packages python build.py",
  "installCommand": "uv pip install --system --only-binary :all: -r requirements.txt --target .vercel_python_packages",
  "outputDirectory": "staticfiles",
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}


